{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["vitest.config.ts", "tooling/vitest-hermetic.ts"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["build", "^build"],
      "outputs": []
    },
    "package": {
      "dependsOn": ["build", "^build"],
      "cache": false,
      "outputs": ["artifacts/**"]
    }
  }
}
