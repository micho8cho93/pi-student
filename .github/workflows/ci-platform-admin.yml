name: CI Platform Admin

on:
  push:
    paths: &paths
      - "apps/platform-admin/**"
      - "packages/organization/**"
      - "packages/contracts/**"
      - "packages/supabase-adapter/**"
      - "packages/classroom/**"
      - "packages/policy/**"
      - "packages/shared/**"
      - "package.json"
      - "package-lock.json"
      - "turbo.json"
      - ".github/workflows/ci-platform-admin.yml"
  pull_request:
    paths: *paths
  workflow_dispatch:

jobs:
  platform-admin:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24.3.0
          cache: npm
      - run: npm ci
      - run: npm run build:platform-admin
      - run: npm run test:platform-admin
      - run: npm run test:architecture
